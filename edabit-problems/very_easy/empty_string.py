#!/usr/bin/env python3

# Create a function that returns True if a string
# is empty and False otherwise.

# Examples:
# is_empty("") --> True
# is_empty(" " ) --> False


def is_empty(str):
    if str == "":
        return True
    else:
        return False
